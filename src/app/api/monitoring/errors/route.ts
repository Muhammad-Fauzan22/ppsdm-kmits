[{"next/server": "import { NextResponse"}, {"total": "number;\n  critical: number;\n  high: number;\n  medium: number;\n  low: number;\n  recent: Array<{\n    id: string;\n    message: string;\n    level: string;\n    timestamp: Date;\n    count: number;"}, {"getTotalErrors": "async () => {\n    // In production, this would come from your error tracking system (e.g., Sentry)\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 100) + 50; // 50-150 total errors"}, {"getCriticalErrors": "async () => {\n    // In production, this would come from your error tracking system\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 5); // 0-4 critical errors"}, {"getHighErrors": "async () => {\n    // In production, this would come from your error tracking system\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 10) + 2; // 2-11 high errors"}, {"getMediumErrors": "async () => {\n    // In production, this would come from your error tracking system\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 20) + 10; // 10-29 medium errors"}, {"getLowErrors": "async () => {\n    // In production, this would come from your error tracking system\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 30) + 20; // 20-49 low errors"}, {"getRecentErrors": "async () => {\n    // In production, this would come from your error tracking system\n    // For now, return simulated recent errors\n    const errorMessages = [\n      'Database connection timeout',\n      'API rate limit exceeded',\n      'Invalid user input',\n      'File upload failed',\n      'Authentication token expired',\n      'Cache miss on critical data',\n      'External service unavailable',\n      'Memory allocation failed',\n    ];\n    \n    const levels = ['critical', 'high', 'medium', 'low'];\n    const recentErrors = [];\n    \n    for (let i = 0; i < 5; i++) {\n      recentErrors.push({\n        id: `error-${Date.now()}-${i}`,\n        message: errorMessages[Math.floor(Math.random() * errorMessages.length)],\n        level: levels[Math.floor(Math.random() * levels.length)],\n        timestamp: new Date(Date.now() - Math.random() * 3600000), // Last hour\n        count: Math.floor(Math.random() * 10) + 1,\n      });\n    }\n    \n    return recentErrors;"}, {"GET": "async () => {\n  try {\n    const [total, critical, high, medium, low, recent] = await Promise.all([\n      getTotalErrors(),\n      getCriticalErrors(),\n      getHighErrors(),\n      getMediumErrors(),\n      getLowErrors(),\n      getRecentErrors(),\n    ]);\n\n    const metrics: ErrorMetrics = {\n      total,\n      critical,\n      high,\n      medium,\n      low,\n      recent,\n    };\n\n    return NextResponse.json(metrics);\n  } catch (error) {\n    console.error('Error fetching error metrics:', error);\n    return NextResponse.json(\n      { error: 'Failed to fetch error metrics' },\n      { status: 500 }\n    );\n  };\n}"}]