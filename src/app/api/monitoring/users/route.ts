[{"next/server": "import { NextResponse"}, {"active": "number;\n  total: number;\n  new: number;\n  returning: number;\n  sessions: number;\n  avgSessionDuration: number;\n  bounceRate: number;"}, {"getActiveUsers": "async () => {\n    // In production, this would come from your analytics system (e.g., Google Analytics)\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 50) + 20; // 20-70 active users"}, {"getTotalUsers": "async () => {\n    // In production, this would come from your database\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 1000) + 500; // 500-1500 total users"}, {"getNewUsers": "async () => {\n    // In production, this would come from your analytics system\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 20) + 5; // 5-25 new users in last 24h"}, {"getReturningUsers": "async () => {\n    // In production, this would come from your analytics system\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 100) + 50; // 50-150 returning users in last 24h"}, {"getSessions": "async () => {\n    // In production, this would come from your analytics system\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 500) + 200; // 200-700 sessions today"}, {"getAvgSessionDuration": "async () => {\n    // In production, this would come from your analytics system\n    // For now, return a simulated value (in seconds)\n    return Math.floor(Math.random() * 600) + 300; // 5-15 minutes"}, {"getBounceRate": "async () => {\n    // In production, this would come from your analytics system\n    // For now, return a simulated value (percentage)\n    return (Math.random() * 30 + 20); // 20-50%"}, {"GET": "async () => {\n  try {\n    const [active, total, newUsers, returning, sessions, avgSessionDuration, bounceRate] = await Promise.all([\n      getActiveUsers(),\n      getTotalUsers(),\n      getNewUsers(),\n      getReturningUsers(),\n      getSessions(),\n      getAvgSessionDuration(),\n      getBounceRate(),\n    ]);\n\n    const metrics: UserMetrics = {\n      active,\n      total,\n      new: newUsers,\n      returning,\n      sessions,\n      avgSessionDuration,\n      bounceRate,\n    };\n\n    return NextResponse.json(metrics);\n  } catch (error) {\n    console.error('Error fetching user metrics:', error);\n    return NextResponse.json(\n      { error: 'Failed to fetch user metrics' },\n      { status: 500 }\n    );\n  };\n}"}]