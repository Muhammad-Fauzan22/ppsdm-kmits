[{"next/server": "import { NextResponse"}, {"lcp": "number;\n  fid: number;\n  cls: number;\n  ttfb: number;\n  fcp: number;\n  si: number;"}, {"getLCP": "async () => {\n    // In production, this would come from Web Vitals or RUM data\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 1000) + 1500; // 1500-2500ms"}, {"getFID": "async () => {\n    // In production, this would come from Web Vitals or RUM data\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 50) + 30; // 30-80ms"}, {"getCLS": "async () => {\n    // In production, this would come from Web Vitals or RUM data\n    // For now, return a simulated value\n    return (Math.random() * 0.1 + 0.01); // 0.01-0.11"}, {"getTTFB": "async () => {\n    // In production, this would come from Web Vitals or RUM data\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 300) + 200; // 200-500ms"}, {"getFCP": "async () => {\n    // In production, this would come from Web Vitals or RUM data\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 800) + 1000; // 1000-1800ms"}, {"getSI": "async () => {\n    // In production, this would come from Web Vitals or RUM data\n    // For now, return a simulated value\n    return Math.floor(Math.random() * 1000) + 2500; // 2500-3500ms"}, {"GET": "async () => {\n  try {\n    const [lcp, fid, cls, ttfb, fcp, si] = await Promise.all([\n      getLCP(),\n      getFID(),\n      getCLS(),\n      getTTFB(),\n      getFCP(),\n      getSI(),\n    ]);\n\n    const metrics: PerformanceMetrics = {\n      lcp,\n      fid,\n      cls,\n      ttfb,\n      fcp,\n      si,\n    };\n\n    return NextResponse.json(metrics);\n  } catch (error) {\n    console.error('Error fetching performance metrics:', error);\n    return NextResponse.json(\n      { error: 'Failed to fetch performance metrics' },\n      { status: 500 }\n    );\n  };\n}"}]