[{"react": "import {\n  getABTestingManager", "@/lib/abtesting/abTesting": "Options untuk useABTesting hook\n */\nexport interface UseABTestingOptions {\n  experimentId: string;\n  userId: string;\n  sessionId: string;\n  autoTrack?: boolean;"}, {"variant": "Variant | null;\n  isLoading: boolean;\n  isAssigned: boolean;\n  experiment: Experiment | undefined;\n  stats: ExperimentStats | null;\n  trackConversion: (customData?: Record<string", "trackEvent": "eventName: string", "value?": "number", "customData?": "Record<string", "refreshStats": "void;\n  getExperiment: () => Experiment | undefined;\n  getAllExperiments: () => Experiment[];\n  getActiveExperiments: () => Experiment[];"}, {"page_view": 1, "customData?": "Record<string", "eventName": "string", "value": "number = 1", "isAssigned": "variant !== null", "experiment": "getExperiment()", "useABTestingStats(experimentId": "string) {\n  const [stats", "experimentIds": "string[]", "userId": "string", "sessionId": "string\n) {\n  const [variants", "newVariants": "Record<string", "experimentId": "string): Variant | null => {\n      return variants[experimentId] || null;"}, ["variants]\n  );\n\n  const trackConversion = useCallback(\n    (experimentId: string, customData?: Record<string, any>) => {\n      const variant = variants[experimentId];\n      if (variant) {\n        trackConversionFromManager(experimentId, userId, sessionId, customData);\n      }\n    },\n    [variants, userId, sessionId]\n  );\n\n  const trackEvent = useCallback(\n    (experimentId: string, eventName: string, value: number = 1, customData?: Record<string, any>) => {\n      const variant = variants[experimentId];\n      if (variant) {\n        trackEventFromManager(experimentId, userId, sessionId, eventName, value, customData);\n      }\n    },\n    [variants, userId, sessionId]\n  );\n\n  return {\n    variants,\n    isLoading,\n    getVariant,\n    trackConversion,\n    trackEvent,\n    refresh: assignVariants,\n  };\n}\n\n/**\n * Hook untuk A/B testing dengan fallback value\n */\nexport function useABTestingWithFallback<T>(\n  experimentId: string,\n  userId: string,\n  sessionId: string,\n  variants: Record<string, T>,\n  fallback: T\n) {\n  const { variant, isLoading, ...rest } = useABTesting({\n    experimentId,\n    userId,\n    sessionId,\n  });\n\n  const variantValue = variant ? variants[variant.id] : fallback;\n\n  return {\n    variant: variantValue,\n    isLoading,\n    isFallback: variant === null,\n    ...rest,\n  };\n}\n\n/**\n * Hook untuk tracking events A/B testing\n */\nexport function useABTestingEvents(\n  experimentId: string,\n  userId: string,\n  sessionId: string\n) {\n  const [events, setEvents] = useState<Array<{ eventName: string; timestamp: Date }>>([]);\n\n  const trackEvent = useCallback(\n    (eventName: string, value: number = 1, customData?: Record<string, any>) => {\n      trackEventFromManager(experimentId, userId, sessionId, eventName, value, customData);\n      setEvents((prev) => [...prev, { eventName, timestamp: new Date() }]);\n    },\n    [experimentId, userId, sessionId]\n  );\n\n  const manager = getABTestingManager();\n  const variant = manager.assignVariant(experimentId, userId, sessionId);\n\n  useEffect(() => {\n    // Track initial page view\n    trackEvent('page_view', 1);\n  }, [trackEvent]);\n\n  return {\n    events,\n    trackEvent,\n  };\n}\n\n/**\n * Hook untuk A/B testing dengan auto-tracking\n */\nexport function useABTestingAutoTrack(\n  experimentId: string,\n  userId: string,\n  sessionId: string\n) {\n  const { variant, isLoading, ...rest } = useABTesting({\n    experimentId,\n    userId,\n    sessionId,\n    autoTrack: true,\n  });\n\n  const trackClick = useCallback(\n    (elementId: string, customData?: Record<string, any>) => {\n      rest.trackEvent('click', 1, { elementId, ...customData });\n    },\n    [rest]\n  );\n\n  const trackScroll = useCallback(\n    (scrollDepth: number, customData?: Record<string, any>) => {\n      rest.trackEvent('scroll', scrollDepth, { scrollDepth, ...customData });\n    },\n    [rest]\n  );\n\n  const trackTimeOnPage = useCallback(\n    (duration: number, customData?: Record<string, any>) => {\n      rest.trackEvent('time_on_page', duration, { duration, ...customData });\n    },\n    [rest]\n  );\n\n  const trackFormSubmit = useCallback(\n    (formId: string, customData?: Record<string, any>) => {\n      rest.trackEvent('form_submit', 1, { formId, ...customData });\n    },\n    [rest]\n  );\n\n  const trackError = useCallback(\n    (error: Error, customData?: Record<string, any>) => {\n      rest.trackEvent('error', 1, { error: error.message, ...customData });\n    },\n    [rest]\n  );\n\n  return {\n    variant,\n    isLoading,\n    ...rest,\n    trackClick,\n    trackScroll,\n    trackTimeOnPage,\n    trackFormSubmit,\n    trackError,\n  };\n}\n\n/**\n * Hook untuk A/B testing admin\n */\nexport function useABTestingAdmin() {\n  const [experiments, setExperiments] = useState<Experiment[]>([]);\n  const [isLoading, setIsLoading] = useState(true);\n\n  const manager = getABTestingManager();\n\n  const refresh = useCallback(() => {\n    setIsLoading(true);\n    setExperiments(manager.getAllExperiments());\n    setIsLoading(false);\n  }, [manager]);\n\n  const createExperiment = useCallback(\n    (experiment: Omit<Experiment, 'status'>) => {\n      const newExperiment = manager.createExperiment(experiment);\n      refresh();\n      return newExperiment;\n    },\n    [manager, refresh]\n  );\n\n  const updateExperiment = useCallback(\n    (experimentId: string, updates: Partial<Experiment>) => {\n      const updatedExperiment = manager.updateExperiment(experimentId, updates);\n      refresh();\n      return updatedExperiment;\n    },\n    [manager, refresh]\n  );\n\n  const deleteExperiment = useCallback(\n    (experimentId: string) => {\n      const result = manager.deleteExperiment(experimentId);\n      refresh();\n      return result;\n    },\n    [manager, refresh]\n  );\n\n  const startExperiment = useCallback(\n    (experimentId: string) => {\n      const result = manager.startExperiment(experimentId);\n      refresh();\n      return result;\n    },\n    [manager, refresh]\n  );\n\n  const pauseExperiment = useCallback(\n    (experimentId: string) => {\n      const result = manager.pauseExperiment(experimentId);\n      refresh();\n      return result;\n    },\n    [manager, refresh]\n  );\n\n  const completeExperiment = useCallback(\n    (experimentId: string) => {\n      const result = manager.completeExperiment(experimentId);\n      refresh();\n      return result;\n    },\n    [manager, refresh]\n  );\n\n  const getExperimentStats = useCallback(\n    (experimentId: string) => {\n      return manager.calculateExperimentStats(experimentId);\n    },\n    [manager]\n  );\n\n  const exportData = useCallback(() => {\n    return manager.exportData();\n  }, [manager]);\n\n  const importData = useCallback(\n    (data: string) => {\n      const result = manager.importData(data);\n      refresh();\n      return result;\n    },\n    [manager, refresh]\n  );\n\n  const clearAll = useCallback(() => {\n    manager.clearAll();\n    refresh();\n  }, [manager, refresh]);\n\n  useEffect(() => {\n    refresh();\n  }, [refresh]);\n\n  return {\n    experiments,\n    isLoading,\n    refresh,\n    createExperiment,\n    updateExperiment,\n    deleteExperiment,\n    startExperiment,\n    pauseExperiment,\n    completeExperiment,\n    getExperimentStats,\n    exportData,\n    importData,\n    clearAll,\n  };\n}\n\n/**\n * Hook untuk A/B testing dengan persistence\n */\nexport function useABTestingWithPersistence(\n  experimentId: string,\n  userId: string,\n  sessionId: string,\n  storageKey?: string\n) {\n  const { variant, isLoading, ...rest } = useABTesting({\n    experimentId,\n    userId,\n    sessionId,\n  });\n\n  const key = storageKey || `ab_testing_${experimentId}_${userId}`;\n\n  // Load variant from storage\n  useEffect(() => {\n    if (isLoading) return;\n\n    try {\n      const stored = localStorage.getItem(key);\n      if (stored && !variant) {\n        // Restore variant from storage\n        const manager = getABTestingManager();\n        const experiment = manager.getExperiment(experimentId);\n        if (experiment) {\n          const storedVariant = experiment.variants.find((v) => v.id === stored);\n          if (storedVariant) {\n            // Re-assign variant\n            manager.assignVariant(experimentId, userId, sessionId);\n          }\n        }\n      }\n    } catch (error) {\n      console.error('[useABTestingWithPersistence] Failed to load from storage:', error);\n    }\n  }, [isLoading, key, experimentId, userId, sessionId, variant]);\n\n  // Save variant to storage\n  useEffect(() => {\n    if (variant) {\n      try {\n        localStorage.setItem(key, variant.id);\n      } catch (error) {\n        console.error('[useABTestingWithPersistence] Failed to save to storage:', error);\n      }\n    }\n  }, [variant, key]);\n\n  return {\n    variant,\n    isLoading,\n    ...rest,\n  };\n}\n\n/**\n * Hook untuk A/B testing dengan server-side rendering\n */\nexport function useABTestingSSR(\n  experimentId: string,\n  userId: string,\n  sessionId: string,\n  serverVariant?: Variant\n) {\n  const [variant, setVariant] = useState<Variant | null>(serverVariant || null);\n  const [isLoading, setIsLoading] = useState(!serverVariant);\n\n  useEffect(() => {\n    if (serverVariant) {\n      setVariant(serverVariant);\n      setIsLoading(false);\n      return;\n    }\n\n    // Client-side assignment\n    const manager = getABTestingManager();\n    const assignedVariant = manager.assignVariant(experimentId, userId, sessionId);\n    setVariant(assignedVariant);\n    setIsLoading(false);\n  }, [experimentId, userId, sessionId, serverVariant]);\n\n  const trackConversion = useCallback(\n    (customData?: Record<string, any>) => {\n      if (variant) {\n        trackConversionFromManager(experimentId, userId, sessionId, customData);\n      }\n    },\n    [variant, experimentId, userId, sessionId]\n  );\n\n  const trackEvent = useCallback(\n    (eventName: string, value: number = 1, customData?: Record<string, any>) => {\n      if (variant) {\n        trackEventFromManager(experimentId, userId, sessionId, eventName, value, customData);\n      }\n    },\n    [variant, experimentId, userId, sessionId]\n  );\n\n  return {\n    variant,\n    isLoading,\n    trackConversion,\n    trackEvent,\n  };\n}"]]